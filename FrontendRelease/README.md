# FrontendRelease

Release-ready front-end bundles live here (e.g., zipped static exports or pinned Streamlit builds) so they stay separate from active development in `FrontendDeveloper/`. Add versioned subfolders or archives as you publish new drops.***
