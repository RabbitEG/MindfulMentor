## Docs 模块指南

本目录是“写清楚大家怎么协作”的地方：存放接口契约、架构说明、调用链讲解。

- 前置知识：
  - Markdown：轻量标记语言，写文档用的 `.md` 语法。
  - API 契约（contract）：定义请求/响应字段、类型和含义，保证服务间对齐。
  - 调用链（call flow）：请求从入口到下游的经过顺序。
- 环境配置：无需运行环境，任意文本编辑器即可。
- 主要任务：维护 `/Docs/Interfaces.md`（字段定义）、`/Docs/Arch.md`（架构/调用链）；当代码字段调整时同步文档，避免“文档跑偏”。
